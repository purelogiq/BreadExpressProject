<% content_for :title, "New Customer" %>
<% content_for :page_heading, "Add a new customer" %>
<% content_for :page_info, "Add a new customer's information to the system." %>
<div class="section white z-depth-5">
  <div class="container">
    <h4 class="left-align"><%= link_to "Customers", customers_path %> > New Customer</h4>
    <div class="divider" style="margin-bottom: 5px"></div>
    <%= render 'form' %>
  </div>
</div>

